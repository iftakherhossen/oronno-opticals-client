import { Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <div style={{my: 'auto'}}>
                <Typography variant="h4" sx={{textAlign: 'center', mt: 10 }}>Payment Coming Soon!</Typography>
            <Typography variant="h6" sx={{textAlign: 'center', mt: 2}}>Now use Cash on Delivery!</Typography>
        </div>
    );
};

export default Pay;